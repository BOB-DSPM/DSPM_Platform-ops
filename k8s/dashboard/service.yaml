apiVersion: v1
kind: Service

metadata:
  name: dashboard 
  namespace: dspm

spec:
  type: LoadBalancer  # AWS ELB를 통한 외부 접근
  selector:
    app: dashboard 
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP